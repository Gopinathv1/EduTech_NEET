'use client';
import { useState } from 'react'; import { useRouter, useSearchParams } from 'next/navigation';
const input='mt-1 w-full rounded-lg border border-border bg-surface px-3 py-2 text-textPrimary';
export function ForgotPasswordForm(){const[email,setEmail]=useState(''),[mobile,setMobile]=useState(''),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,mobile})});setMsg(r.ok?'If the details match an account, a password reset link has been sent to the registered email address.':'Enter a valid email and mobile number.')}return <form onSubmit={go} className="space-y-4"><label className="block text-sm font-medium">Registered email *<input required type="email" className={input} value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm font-medium">Registered mobile number *<input required className={input} value={mobile} onChange={e=>setMobile(e.target.value)}/></label><button className="w-full rounded-lg bg-brand px-4 py-2 font-semibold text-white">Send Reset Link</button>{msg&&<p className="text-sm text-textSecondary">{msg}</p>}</form>}
export function ResetPasswordForm(){const p=useSearchParams(),r=useRouter(),[password,setPassword]=useState(''),[confirmPassword,setConfirm]=useState(''),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();const x=await fetch('/api/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:p.get('token'),password,confirmPassword})});if(x.ok)r.push('/login');else setMsg('This reset link is invalid or expired.')}return <form onSubmit={go} className="space-y-4"><label className="block text-sm font-medium">New password *<input required minLength={8} type="password" className={input} value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="block text-sm font-medium">Confirm password *<input required type="password" className={input} value={confirmPassword} onChange={e=>setConfirm(e.target.value)}/></label><button className="w-full rounded-lg bg-brand px-4 py-2 font-semibold text-white">Reset Password</button>{msg&&<p className="text-sm text-textSecondary">{msg}</p>}</form>}
